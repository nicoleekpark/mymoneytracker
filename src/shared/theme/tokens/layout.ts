/**
 * Layout constants for consistent component sizing.
 */

/**
 * Category breakdown row layout dimensions.
 * Used in MonthlyCategorySection, MonthlyIncomeSection, AllBody, CategoryAccordion.
 */
export const CATEGORY_ROW_LAYOUT = {
  /** Width for percentage display column (e.g., "42%") */
  percentageColumnWidth: 40,
  /** Width for chevron/spacer column */
  chevronColumnWidth: 20,
} as const
